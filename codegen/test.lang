class ROBox<+T> {
  // Hello world
  public #T data;

  function get() : #T {
    let $ret = $this->data;
  } return $ret
}

class Box<T>
  where (#T as arraykey)
{
    public #T data;

    function get() : #T {
        let $ret = $this->data
    } return $ret;

    function set(#T $x): null {
        $this->data = $x
    }
    return null;
}

class IntBoxS<> extends Box<int> {
    function set(int $data) : null {
        $this->data = $data + 1
    }
    return null
}

class Main<> {
    function main(): bool {
        let $robox = new ROBox<>($data = 42);
        let $init = $robox->get();
        let $box = new IntBoxS<>($data = $init);
        let $tmp = $box->get();
        let $tmp = $tmp + 20;
        let $tmp0 = $box->set($x = $tmp - 10);
        let $tmp = $box->data
    } return $tmp = 43
}
